name: CRC50 temporary download
on:
  push:
    branches: ['temp/crc50-download-20260918']
    paths: ['.github/workflows/crc50-download.yml', 'crc50/**']
permissions:
  contents: read
jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Install PDF validation and artifact encryption dependencies
        run: python -m pip install --disable-pip-version-check requests pypdf cryptography
      - name: Retrieve public originals and verify identity
        run: python -u crc50/download.py
      - name: Upload encrypted temporary bundle
        uses: actions/upload-artifact@v4
        with:
          name: CRC50-encrypted-originals
          path: encrypted_artifact/
          retention-days: 1
          if-no-files-found: error
          compression-level: 0
